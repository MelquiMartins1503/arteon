generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["postgresqlExtensions"]
}

datasource db {
  provider   = "postgresql"
  schemas    = ["public"]
  extensions = [pg_graphql, pg_stat_statements, pgcrypto, pgjwt, supabase_vault, uuid_ossp(map: "uuid-ossp"), vector]
}
